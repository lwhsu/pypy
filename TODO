TODO:
 - check for sufficient memory
 * add test target
 * support -O0 -O1 -Osize (gc)
 - support CLI backend (broken in 1.5)
 - support JVM backend
 - support valgrind
 * use the first instance to compile object code
 - make jitviewer port (depends flask pygments simplejson Jinja2 Werkzeug)
 * add pkg-plist target
 * add config options (stackless and sandbox)
 * port test
 * fix detection of expat.h
